// Site settings for Sky Aquarium.
// relay: the Sky Aquarium relay link (see ../relay/README.md). Every visitor uses it unless they
// paste their own in the menu. Leave empty to have each visitor set their own.
window.SKY_AQUARIUM = {
  relay: '',
};
